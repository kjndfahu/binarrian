import {AvatarBlock} from "@/features/dashboard/settings/avatar-block.tsx";
import {ProfileInputs} from "@/features/dashboard/settings/profile-inputs.tsx";

export function ProfileSection(){
    return (
        <>
            <AvatarBlock/>
            <ProfileInputs/>
        </>
    )
}